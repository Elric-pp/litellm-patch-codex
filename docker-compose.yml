services:
  litellm:
    image: litellm-patched
    ports:
      - "4000:4000"
    volumes:
      - ./config.yaml:/app/config.yaml
    env_file:
      - .env
    command:
      - "--config=/app/config.yaml"
      - "--detailed_debug"
